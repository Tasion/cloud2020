server:
  port: 80
spring:
  application:
    name: cloud-order-service
eureka:
  client:
    #表示自己是否注册进eureka 服务，默认ture
    register-with-eureka: true
    #
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka